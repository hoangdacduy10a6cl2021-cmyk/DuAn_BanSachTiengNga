@using QuanLySach.Models
@model Category
@{
    Layout = "_QuanTriLayout";
    ViewBag.Title = "Sửa danh mục";
}

<div class="admin-panel">
    <form asp-action="CategoryEdit" asp-route-id="@Model.Id" method="post" class="admin-form">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <label>Tên danh mục</label>
            <input type="text" asp-for="Name" required />
        </div>
        <button type="submit" class="btn btn-gold">Cập nhật</button>
        <a asp-action="Categories" class="btn btn-outline">Hủy</a>
    </form>
</div>