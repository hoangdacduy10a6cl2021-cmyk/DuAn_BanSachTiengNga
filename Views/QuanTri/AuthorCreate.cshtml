@using QuanLySach.Models
@model Author
@{
    Layout = "_QuanTriLayout";
    ViewBag.Title = "Добавить автора";
}

<div class="admin-panel">
    <form asp-action="AuthorCreate" method="post" class="admin-form">
        @Html.AntiForgeryToken()
        <div class="form-group">
            <label>Имя автора</label>
            <input type="text" asp-for="Name" required autofocus />
        </div>
        <div class="form-group">
            <label>Биография</label>
            <textarea asp-for="Bio" rows="4"></textarea>
        </div>
        <div class="form-group">
            <label>Ссылка на фото (необязательно)</label>
            <input type="text" asp-for="PhotoUrl" />
        </div>
        <button type="submit" class="btn btn-gold">Сохранить</button>
        <a asp-action="Authors" class="btn btn-outline">Отмена</a>
    </form>
</div>